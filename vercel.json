{
  "framework": "vite",
  "buildCommand": "pnpm --filter @tow/web build",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
